# This file is .gitignore for file and directory signatures that are specific
# to my environment / workflow and which should not be checked into repositories
#
# NOTE: directory/    excludes the directory entry itself
#       directory/**  excludes everything contained in that directory.

# miscellaneous material for examples or to incorporate in the future.
# archive/
# archive/**

# .idea directory created by CLion (or other JetBrains products)
# .idea/
# .idea/**

# .orig files are generated by beyond compare when doing merges.
*.orig


# root directory clion configures for out-of-source builds
cmake-build-debug/
cmake-build-debug/**
cmake-build-debug-event-trace/
cmake-build-debug-event-trace/**

.metadata_never_index
.DS_store
